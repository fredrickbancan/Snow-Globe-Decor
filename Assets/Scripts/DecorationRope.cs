using UnityEngine;

//TODO: physics simulation for a set amount of time and then ceasing all simulation but leaving rope and decor static in scene.
public class DecorationRope : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
